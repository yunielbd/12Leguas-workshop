import { Routes } from '@angular/router';
import { VehicleEntryFormComponent } from './vehicle-entry-form.component';

export const entryRoutes: Routes = [
  { path: 'entry', component: VehicleEntryFormComponent },
];